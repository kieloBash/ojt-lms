"use client";
import { UserType } from "@/lib/interfaces/user.interface";
import React from "react";

const TeacherComponent = ({ userInfo }: { userInfo: UserType }) => {
  return <div>TeacherComponent</div>;
};

export default TeacherComponent;
